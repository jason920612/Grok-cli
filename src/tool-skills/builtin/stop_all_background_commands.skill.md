# stop_all_background_commands

Stop all background commands started by this agent session.

- Use for one-shot cleanup unless the user explicitly asked to keep processes running.
- Report cleanup status in the final answer.
