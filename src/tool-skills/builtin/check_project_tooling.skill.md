# check_project_tooling

Inspect project-local scripts, lockfiles, dependencies, and recommended commands.

- Use before test, build, lint, format, typecheck, dev server, or missing tool installation.
- Prefer local scripts and devDependencies.
- Do not assume npm.
- Consider global setup only when no project-local option exists.
