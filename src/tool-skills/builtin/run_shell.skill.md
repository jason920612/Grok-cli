# run_shell

Run bounded foreground commands.

- Use for inspection, tests, builds, lint, and type checks.
- Inspect environment and project tooling first.
- Prefer project-local commands.
- Do not perform global installs, shell profile edits, sudo, brew, apt, deploy, or publish without explicit approval.
- Do not use shell to read large files.
