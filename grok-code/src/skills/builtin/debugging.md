# Debugging Skill

Use evidence before changing code.

Rules:
1. Capture the exact error, stack trace, failing command, or failing test.
2. Search for the error string and relevant symbols.
3. Read the failing code path and nearest tests.
4. Make the smallest patch that explains the failure.
5. Re-run the smallest relevant check.
